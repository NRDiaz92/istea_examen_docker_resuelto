pipeline {
    agent any

    stages {
        stage('Build') {
            parallel {
                stage('Build: api') {
                    steps {
                        sh "sudo docker build -f ${WORKSPACE}/istea_examen_docker_resuelto/AppCode/Dockerfile.python ${WORKSPACE}/istea_examen_docker_resuelto/AppCode/api/"
                    }
                }
                stage('Build: redis') {
                    steps {
                        sh "sudo docker build -f ${WORKSPACE}/istea_examen_docker_resuelto/AppCode/Dockerfile.python ${WORKSPACE}/istea_examen_docker_resuelto/AppCode/redis/"
                    }
                }
                stage('Build: worker') {
                    steps {
                        sh "sudo docker build -f ${WORKSPACE}/istea_examen_docker_resuelto/AppCode/Dockerfile.python ${WORKSPACE}/istea_examen_docker_resuelto/AppCode/worker/"
                    }
                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}